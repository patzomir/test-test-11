Generate DataProduct111 Docs:
  extends: 
    - .modelling_and_transformation_base
    - .agent_tag
  variables:
    TRANSFORM_ACTION: DOCS
    TRANSFORM_MODEL_SELECTOR: tag:data_product_111
    DATAOPS_DATA_PRODUCT_SPECIFICATION_FILE: ${CI_PROJECT_DIR}/${DATA_PRODUCT_SOURCE_FILE}
  stage: "Generate Docs"
  script:
    - /dataops
  artifacts:
    when: always
    name: modelling_and_transformation
  icon: ${TRANSFORM_ICON}
